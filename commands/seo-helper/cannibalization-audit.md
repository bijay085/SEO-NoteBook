# seo-cannibalization-audit

Forensic keyword-cannibalization audit from GSC data. Runs a decision cascade per URL pair (redirect / merge / keep / monitor) with shared queries as evidence.

Load and follow the full skill instructions:

`${CLAUDE_PLUGIN_ROOT}/skills/seo-cannibalization-audit/SKILL.md`

After reading the SKILL.md, follow its workflow exactly. Use the skill's reference files and scripts as instructed within it.
