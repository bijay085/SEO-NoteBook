# Consolidated Python dependencies for seo-* skills.
# Generated 2026-08-03 from the actual imports across every seo-*/scripts/*.py file.
# Versions pinned below are what's confirmed installed and working on the source
# machine (python3.14 via Homebrew) -- run `python3 --version` on the new device
# and adjust if it's on an older interpreter.
#
# Re-checked 2026-08-03 after adding seo-cannibalization-audit (numpy + pandas +
# openpyxl, no scipy by design) and seo-log-file-analysis (stdlib engine, openpyxl
# only for the XLSX) -- no new packages needed, everything below already covers both.

beautifulsoup4==4.15.0
Jinja2==3.1.6
lxml==6.1.1
numpy==2.4.4
openpyxl==3.1.5
pandas==3.0.2
pydantic==2.13.4
requests==2.34.2

# Used only by seo-ecom-decline-investigation/scripts/period_decomposition.py.
# NOT pinned -- these are not installed anywhere on the source machine either
# (checked both the Homebrew python3.14 and python3.12 interpreters), so that
# one script's statistical-decomposition path is currently unverified even at
# the source. Install before relying on it:
scipy
statsmodels

# Optional, seo-render-audit only -- skip unless you want local JS rendering
# instead of supplying a rendered-DOM file via the browser / Playwright tool:
# playwright>=1.44.0   (then: playwright install chromium)
